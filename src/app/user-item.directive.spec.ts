import { UserItemDirective } from './user-item.directive';

describe('UserItemDirective', () => {
  it('should create an instance', () => {
    const directive = new UserItemDirective();
    expect(directive).toBeTruthy();
  });
});
